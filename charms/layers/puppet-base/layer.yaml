repo: https://github.com/jamesbeedy/layer-puppet-base.git
includes:
  - 'layer:basic'
  - 'layer:apt'
defines:
  puppet-srvc:
    type: string
    default: ''
    description: |
      Puppet pkg options are `master`, `agent`, `standalone`, `db`, `ca`.
